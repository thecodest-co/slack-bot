CREATE SCHEMA IF NOT EXISTS slack_bot;

CREATE TABLE slack_bot.applications
(
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE,
    url  VARCHAR(65535)
);
